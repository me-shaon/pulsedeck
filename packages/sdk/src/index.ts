/**
 * PulseDeck client SDK — stub.
 *
 * The real SDK (typed report builder + transport) ships in v1.1.
 * This placeholder keeps the package present and buildable in the workspace.
 */
export const SDK_PLACEHOLDER = true as const;
